syntax = "proto3";

enum UserType {
  USER_TYPE_UNSPECIFIED = 0;
  USER_TYPE_ADMIN = 1;
  // Standard user is defined as lowest privilege user the system supports.
  USER_TYPE_STANDARD = 2;
}

message User {
  string name = 1;
  string email = 2;
}